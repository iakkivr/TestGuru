<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to t('helpers.link.show'), admin_test_path(test) %></td>
  <td><%= link_to t('helpers.link.edit'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('helpers.link.delete'), admin_test_path(test), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
