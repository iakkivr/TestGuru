<div class="row justify-content-center">
  <div class="col col-md-8">
    <h2 class="text-center">Gists</h2>

    <table class="table table-hover mt-4">
      <tr class="fs-5">
        <th scope="col"><%= User.model_name.human %></th>
        <th scope="col"><%= Question.model_name.human %></th>
        <th scope="col">URL</th>
      </tr>
      <%= render @gists %>
    </table>
  </div>
</div>
