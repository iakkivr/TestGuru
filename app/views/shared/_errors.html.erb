<% if  object.present? && object.errors.any?  %>
  <div class="alert alert-danger">
    <h2>
        <%= t('.header', count: object.errors.count) %>:
    </h2>

    <ul class="mb-0">
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
